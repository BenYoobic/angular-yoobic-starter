//import ScrollReveal from 'scrollreveal';
//let sr: any;
// if (ScrollReveal) {
//     sr = ScrollReveal({ reset: true });
// }
export function scrollrevealReveal(childSelector, container, options = {}) {
    // if (sr) {
    //     sr.reveal(childSelector, {
    //         container,
    //         ...options,
    //         opacity: 0, // isIOS() && isCordova() ? null : 0,
    //         scale: 0.9,
    //         useDelay: 'onload'
    //         // interval: 50,
    //         // rotation: { z: 65 }
    //     });
    // }
}
// export function scrollrevealClean(childSelector: string | Node | NodeList | Array<Node>) {
//     if (sr) {
//         sr.clean(childSelector);
//     }
// }
export function scrollrevealSync(timeout = 0) {
    // if (sr) {
    //     setTimeout(() => {
    //         sr.sync();
    //     }, timeout);
    //}
}
// export function destroyAllReveal() {
//     if (sr) {
//         sr.destroy();
//     }
// }
