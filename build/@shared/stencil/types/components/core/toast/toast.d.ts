import '../../../stencil.core';
import { EventEmitter } from '../../../stencil.core';
import { IToastEntry, ToastPosition, IEntityAction, IEntitiesAction } from '../../../interfaces';
import { OverlayInterface, OverlayEventDetail } from '../../../utils/ionic';
export declare class YooToastComponent implements OverlayInterface, IToastEntry {
    message: string;
    icon: string;
    count: number;
    image: string;
    cssClass: string;
    closeButtonText: string;
    showCloseButton: boolean;
    duration: number;
    position: ToastPosition;
    progressEvent: any;
    showBackdrop: boolean;
    extraButtons: Array<IEntityAction | IEntitiesAction>;
    willAnimate: boolean;
    keyboardClose: boolean;
    overlayId: number;
    didPresent: EventEmitter<void>;
    willPresent: EventEmitter<void>;
    willDismiss: EventEmitter<OverlayEventDetail>;
    didDismiss: EventEmitter<OverlayEventDetail>;
    progress: number;
    renameValue: string;
    host: HTMLStencilElement;
    presented: boolean;
    animation?: Animation;
    private durationTimeout;
    private usersElement?;
    private outerContainerDiv;
    protected lifecycle(modalEvent: CustomEvent): void;
    present(): Promise<void>;
    dismiss(data?: any, role?: string, overlayDismiss?: boolean): Promise<void>;
    onDidDismiss(callback?: (detail: OverlayEventDetail) => void): Promise<OverlayEventDetail>;
    onWillDismiss(callback?: (detail: OverlayEventDetail) => void): Promise<OverlayEventDetail>;
    componentWillLoad(): void;
    updateProgress(progress: number): void;
    onButtonClick(): void;
    renderCount(): JSX.Element;
    renderImage(): JSX.Element;
    renderIcon(): JSX.Element;
    renderExtraButtons(extraButton: any): JSX.Element;
    hostData(): {
        class: any;
    };
    render(): JSX.Element;
}
